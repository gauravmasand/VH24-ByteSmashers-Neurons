const sendOtpToPhone = (phone, otp) => {
    // Placeholder function to simulate OTP sending.
    // Replace this with actual integration with an SMS provider.
    console.log(`Sending OTP ${otp} to phone number ${phone}`);
  };
  
  module.exports = { sendOtpToPhone };